
function currentTime() {
  return new Date().toLocaleString();
}

function daysBetween(date1, date2) {
  const diff = new Date(date2) - new Date(date1);
  return diff / (1000 * 60 * 60 * 24);
}

module.exports = { currentTime, daysBetween };
